import { ArgTypes, Canvas, Meta } from "@storybook/blocks";

import * as PopoverStories from "./mt-popover.stories";
import StorybookPageHeader from "../../docs/_components/storybook-page-header.js";

<Meta of={PopoverStories} />

<StorybookPageHeader
  title="Popover"
  tagName="mt-popover"
  status="experimental"
  packageImports="MtPopover"
  sourcePath="packages/component-library/src/components/mt-popover"
>
  **Popover** is experimental. Prefer **Action Menu** or **Floating UI** when
  those components already fit the use case.
</StorybookPageHeader>

## Replacement guidance

- Use **Action Menu** when you need a standard menu of actions or options.
- Use **Floating UI** when you need a custom floating surface with your own layout, content, or interaction behavior.

## Example

<Canvas of={PopoverStories.Default} />

## API reference

<ArgTypes of={PopoverStories} />
