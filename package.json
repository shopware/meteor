{
  "name": "meteor",
  "version": "0.0.0",
  "private": true,
  "description": "The monorepo that contains all Meteor related projects",
  "scripts": {
    "lint:all": "turbo run lint:eslint",
    "lint:types": "turbo run lint:types",
    "lint:eslint": "turbo run lint:eslint",
    "format": "turbo run format"
  },
  "keywords": [],
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.12.2"
  }
}
