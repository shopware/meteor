---
phase: 01-lesson-catalog-authoring-model
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - packages/admin-sdk-tutorial/src/App.vue
  - packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts
autonomous: true
requirements:
  - LESS-01
must_haves:
  truths:
    - A browser user can open the tutorial package and see the ordered lesson catalog.
    - The rendered list comes from the normalized catalog loader, not hardcoded lesson rows.
    - Each visible lesson exposes canonical docs links and editable-file metadata without introducing editor or runtime controls.
  artifacts:
    - packages/admin-sdk-tutorial/src/App.vue
    - packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts
  key_links:
    - `App.vue` consumes `loadLessonCatalog()` from Plan 03.
    - The Playwright spec verifies the rendered order against authored lesson titles and docs-link targets.
---

<objective>
Render the ordered lesson catalog in the browser and prove `LESS-01` with a focused Playwright smoke check.

Purpose: Close the phase with a visible outcome while keeping the UI intentionally narrow: ordered lesson browsing only, no editor, preview runtime, or fake-admin behavior.
Output: Minimal catalog UI in `App.vue` plus a targeted browser test that asserts the rendered order and canonical docs links.
</objective>

<execution_context>
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-lesson-catalog-authoring-model/01-CONTEXT.md
@.planning/phases/01-lesson-catalog-authoring-model/01-RESEARCH.md
@.planning/phases/01-lesson-catalog-authoring-model/01-VALIDATION.md
@packages/admin-sdk-tutorial/src/catalog/index.ts
@packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts
@packages/admin-sdk-tutorial/src/catalog/catalog.spec.ts
@packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the ordered lesson list from the normalized catalog</name>
  <files>packages/admin-sdk-tutorial/src/App.vue</files>
  <read_first>packages/admin-sdk-tutorial/src/App.vue, packages/admin-sdk-tutorial/src/catalog/index.ts, packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts, .planning/phases/01-lesson-catalog-authoring-model/01-CONTEXT.md, .planning/phases/01-lesson-catalog-authoring-model/01-RESEARCH.md</read_first>
  <action>Replace the placeholder `App.vue` with a minimal, content-led catalog view. Call `loadLessonCatalog()` during setup and render nested sections for parts and chapters using explicit headings and ordered lesson rows. Each lesson row must show the lesson title, scenario kind, exact `primaryEditableFile` name, readonly support-file count, and one or more docs-link anchors from the manifest. Add stable selectors: `data-testid="tutorial-part"` on each part wrapper, `data-testid="tutorial-chapter"` on each chapter wrapper, `data-testid="lesson-list-item"` on each lesson row, and `data-testid="lesson-doc-link"` on each docs anchor. Do not add Monaco/CodeMirror, split panes, preview iframes, run buttons, restore buttons, or fake-admin host chrome in this plan.</action>
  <verify><automated>pnpm --dir packages/admin-sdk-tutorial test:unit -- --run</automated></verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/src/App.vue` contains `loadLessonCatalog()`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `data-testid="tutorial-part"`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `data-testid="tutorial-chapter"`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `data-testid="lesson-list-item"`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `data-testid="lesson-doc-link"`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `primaryEditableFile`
  </acceptance_criteria>
  <done>The browser app renders the ordered catalog from real lesson data only, and the UI remains within the narrow list-only scope for Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the browser scaffold with a focused ordered-list smoke test</name>
  <files>packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts</files>
  <read_first>packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts, packages/admin-sdk-tutorial/src/App.vue, packages/admin-sdk-tutorial/playwright.config.ts, packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts, .planning/phases/01-lesson-catalog-authoring-model/01-VALIDATION.md</read_first>
  <action>Replace the skipped shell test with a real Playwright spec. Visit `/`, assert there is one `tutorial-part` section, assert the lesson row order is `Dispatch your first notification` then `Understand hidden locations`, and assert docs-link `href` values include `/guide/api-reference/notification`, `/guide/concepts/locations`, and `/guide/concepts/positions`. Use locator assertions only. Keep the task verify command focused on this single browser smoke test so feedback latency stays stable as the suite grows.</action>
  <verify><automated>pnpm --dir packages/admin-sdk-tutorial exec playwright test tests/e2e/lesson-catalog.spec.ts</automated></verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts` contains `Dispatch your first notification`
    - `packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts` contains `Understand hidden locations`
    - `packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts` contains `/guide/api-reference/notification`
    - `packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts` contains `/guide/concepts/locations`
    - `packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts` contains `/guide/concepts/positions`
  </acceptance_criteria>
  <done>`LESS-01` is enforced by an automated browser test that would fail if lesson ordering or docs-link exposure regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized catalog -> rendered browser UI | The visible lesson list trusts catalog output for order, docs links, and lesson metadata |
| docs-link metadata -> anchor rendering | Rendered links must stay tied to canonical tutorial-approved docs targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `packages/admin-sdk-tutorial/src/App.vue` | mitigate | Render lesson rows from `loadLessonCatalog()` only and verify the concrete order in Playwright so hardcoded or stale UI content cannot silently replace the normalized catalog |
| T-04-02 | Information Disclosure | `packages/admin-sdk-tutorial/src/App.vue` | mitigate | Render docs links from validated manifest metadata only and assert the exact `/guide/...` hrefs in the browser smoke test |
| T-04-03 | Denial of Service | `packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts` | mitigate | Use deterministic selectors and a targeted single-spec Playwright command so CI feedback remains fast and reliable |
</threat_model>

<verification>
- `pnpm --dir packages/admin-sdk-tutorial test:unit -- --run`
- `pnpm --dir packages/admin-sdk-tutorial exec playwright test tests/e2e/lesson-catalog.spec.ts`
- `pnpm --dir packages/admin-sdk-tutorial lint:eslint`
- `pnpm --dir packages/admin-sdk-tutorial lint:types`
- Final repo-required hygiene after all Phase 1 changes: `pnpm --dir packages/admin-sdk-tutorial lint:eslint:fix`, then re-run `pnpm --dir packages/admin-sdk-tutorial lint:eslint`
- Final repo-required Jest step after all Phase 1 changes: `pnpm --dir packages/admin-sdk test:unit`
</verification>

<success_criteria>
- Opening the tutorial package in the browser shows an ordered `parts -> chapters -> lessons` list
- Every rendered lesson row comes from the normalized catalog and shows its primary editable file plus canonical docs-link metadata
- The final phase verification includes both unit and focused browser coverage, satisfying `LESS-01` without broadening scope
- No editor shell, preview iframe, fake-admin host, or run controls are added in Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/01-lesson-catalog-authoring-model/01-04-SUMMARY.md`
</output>
