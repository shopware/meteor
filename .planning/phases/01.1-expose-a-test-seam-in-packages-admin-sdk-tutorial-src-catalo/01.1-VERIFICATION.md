---
phase: 01.1-expose-a-test-seam-in-packages-admin-sdk-tutorial-src-catalo
verified: 2026-04-10T12:56:36Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 01.1: Loader Seam Verification Report

**Phase Goal:** Create a narrow loader seam that keeps the runtime catalog behavior unchanged while making the missing-asset error branch automatable in tests without mutating seeded lesson fixtures.
**Verified:** 2026-04-10T12:56:36Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | The missing-asset loader branch is covered by an automated test that does not mutate seeded lesson fixtures. | ✓ VERIFIED | `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` builds synthetic module maps only and asserts the exact `Missing lesson asset "support/missing.ts" in bundle "../lessons/part-99-test/chapter-01-seam/lesson-01-missing-asset"` error. |
| 2 | `loadLessonCatalog()` still loads the seeded lesson catalog through the default eager Vite glob modules and keeps the runtime catalog shape from Phase 01. | ✓ VERIFIED | `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` now delegates `loadLessonCatalog()` to `loadLessonCatalogFromModules(defaultLessonCatalogModules)`, preserving the existing eager manifest and raw asset globs; `packages/admin-sdk-tutorial/src/catalog/catalog.spec.ts` remains green against seeded lessons. |
| 3 | The seam does not weaken existing path-safety or missing-asset error behavior. | ✓ VERIFIED | The new helper still routes all injected modules through `createLessonCatalogEntry()`, `readLessonBundleFile()`, and `resolveLessonBundleFilePath()` in `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts`, so relative-path validation and fail-fast missing-asset throws are shared across runtime and tests. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` | Default glob-backed loader seam plus injectable module-map helper | ✓ VERIFIED | Defines `defaultLessonCatalogModules`, exports `loadLessonCatalogFromModules()`, and keeps `loadLessonCatalog()` wired to the default eager lesson globs. |
| `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` | Automated missing-asset coverage using synthetic module maps | ✓ VERIFIED | Adds a positive synthetic-bundle regression and a negative missing-asset regression without touching `src/lessons/**`. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` | default eager lesson globs | `defaultLessonCatalogModules` | ✓ WIRED | The runtime entrypoint still uses `import.meta.glob("../lessons/**/lesson.manifest.ts")` and `import.meta.glob("../lessons/**/*.{md,ts,js,vue,json}", { query: "?raw" })`. |
| `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` | `packages/admin-sdk-tutorial/src/catalog/normalizeCatalog.ts` | `return normalizeCatalog(lessonEntries)` | ✓ WIRED | Both the runtime path and the seam helper return normalized entries through the existing normalizer. |
| `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` | real bundle-path derivation logic | synthetic manifest path | ✓ WIRED | The spec uses `../lessons/part-99-test/chapter-01-seam/lesson-01-missing-asset/lesson.manifest.ts`, so bundle resolution still comes from the production path derivation. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Tutorial package ESLint autofix | `node /Users/jannisleifeld/.volta/tools/image/packages/pnpm/lib/node_modules/pnpm/bin/pnpm.cjs --dir packages/admin-sdk-tutorial lint:eslint:fix` | Exit code `0` and no post-fix diff remained in the package | ✓ PASS |
| Tutorial package ESLint | `node /Users/jannisleifeld/.volta/tools/image/packages/pnpm/lib/node_modules/pnpm/bin/pnpm.cjs --dir packages/admin-sdk-tutorial lint:eslint` | Exit code `0` | ✓ PASS |
| Tutorial package typecheck | `node /Users/jannisleifeld/.volta/tools/image/packages/pnpm/lib/node_modules/pnpm/bin/pnpm.cjs --dir packages/admin-sdk-tutorial lint:types` | Exit code `0` | ✓ PASS |
| Tutorial package unit suite | `node /Users/jannisleifeld/.volta/tools/image/packages/pnpm/lib/node_modules/pnpm/bin/pnpm.cjs --dir packages/admin-sdk-tutorial test:unit -- --run` | `4` test files passed, `11` tests passed | ✓ PASS |
| Requested direct Jest invocation | `node /Users/jannisleifeld/.volta/tools/image/packages/pnpm/lib/node_modules/pnpm/bin/pnpm.cjs --dir packages/admin-sdk-tutorial exec jest --collectCoverage --watchman=false` | Fails because this package has no Jest transform/config for its Vitest and Playwright ESM specs | ! NON-BLOCKING TOOLING GAP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `QUAL-01` | `01.1-01-PLAN.md` | Tutorial catalog loader missing-asset failures are covered by automated tests through an injectable loader seam that leaves seeded lesson fixtures unchanged and preserves the default runtime catalog path. | ✓ SATISFIED | The seam lives in `loadLessonCatalog.ts`, the synthetic coverage lives in `loadLessonCatalog.spec.ts`, seeded-fixture integration coverage stays green in `catalog.spec.ts`, and no file under `packages/admin-sdk-tutorial/src/lessons/` changed in this phase. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| None | - | No blocking TODOs, placeholder branches, or widened public exports were introduced. | - | No anti-pattern blocks the phase goal. |

### Human Verification Required

None.

### Gaps Summary

No implementation gaps remain against `QUAL-01`. The only residual issue is a package-level tooling mismatch for the extra direct Jest command: `packages/admin-sdk-tutorial` is currently validated with ESLint, `vue-tsc`, Vitest, and Playwright, not Jest transforms. That does not block the loader-seam goal or the new automated missing-asset coverage.

---

_Verified: 2026-04-10T12:56:36Z_
_Verifier: Codex (inline execute-phase verification)_
