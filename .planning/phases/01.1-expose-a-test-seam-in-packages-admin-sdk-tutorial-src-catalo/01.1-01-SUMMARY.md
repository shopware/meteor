---
phase: 01.1-expose-a-test-seam-in-packages-admin-sdk-tutorial-src-catalo
plan: 01
subsystem: catalog
tags: [tutorial, vitest, catalog, seam, coverage]
requires:
  - phase: 01-03
    provides: glob-backed lesson catalog loading, path guards, normalized catalog output
provides:
  - injectable module-map seam for the lesson catalog loader
  - automated missing-asset coverage without touching seeded fixtures
affects: [01.1-verification, tutorial-catalog, future-loader-tests]
tech-stack:
  added: []
  patterns: [defaulted loader seam, synthetic module-map coverage for catalog branches]
key-files:
  created:
    - packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts
  modified:
    - packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts
key-decisions:
  - "Kept the new seam local to `loadLessonCatalog.ts` so package consumers still use the existing public entrypoint."
  - "Routed injected modules through the existing bundle-path and entry-construction helpers so test and runtime behavior stay aligned."
patterns-established:
  - "Loader seam pattern: default runtime globs live beside an exported helper that accepts injected module maps for branch-focused tests."
requirements-completed: [QUAL-01]
duration: 4min
completed: 2026-04-10
---

# Phase 01.1 Plan 01: Loader Seam Summary

**A defaulted catalog-loader seam now enables automated missing-asset coverage while preserving the production glob-backed lesson path**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-10T12:53:00Z
- **Completed:** 2026-04-10T12:56:36Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `loadLessonCatalogFromModules()` and a `defaultLessonCatalogModules` object so tests can inject synthetic module maps without changing the runtime entrypoint.
- Preserved the existing bundle-path guards, missing-asset errors, and normalization flow by routing both runtime and test inputs through the same internal helpers.
- Added focused Vitest coverage for both a successful synthetic lesson bundle and the exact `Missing lesson asset ...` error branch.

## Task Commits

Each task was committed atomically:

1. **Task 1: Refactor `loadLessonCatalog.ts` behind a defaulted module-map seam** - `21e7b13c` (feat)
2. **Task 2: Add focused automated coverage for the missing-asset branch without mutating fixtures** - `9c60f09d` (test)

## Files Created/Modified
- `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` - defines the default module-map object and the injectable loader seam while keeping the public runtime function unchanged
- `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` - exercises the seam with synthetic lesson inputs and verifies the exact missing-asset failure

## Decisions Made
- Exported the seam from `loadLessonCatalog.ts` only and left `src/catalog/index.ts` untouched to avoid widening the package surface.
- Reused a synthetic manifest path under `../lessons/.../lesson.manifest.ts` so tests still exercise the real bundle-path derivation logic.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The default `pnpm` shim on this machine goes through Corepack, which currently fails signature validation for `pnpm@10.12.3`. Verification used the already-installed local `pnpm.cjs` binary instead.
- The requested direct Jest invocation is not configured for `packages/admin-sdk-tutorial`; it attempts to execute the package's Vitest and Playwright ESM specs as raw Jest suites and fails before running tests. This is a package-level tooling mismatch, not a regression from this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase `01.1` closes the missing-asset coverage gap called out after Phase `01`.
- Future catalog changes can add loader-edge-case tests through injected module maps instead of mutating seeded lesson fixtures.

## Self-Check: PASSED

- FOUND: `.planning/phases/01.1-expose-a-test-seam-in-packages-admin-sdk-tutorial-src-catalo/01.1-01-SUMMARY.md`
- FOUND: `21e7b13c`
- FOUND: `9c60f09d`
- FOUND: `QUAL-01`
