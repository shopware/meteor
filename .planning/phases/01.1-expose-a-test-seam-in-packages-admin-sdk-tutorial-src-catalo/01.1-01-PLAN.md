---
phase: 01.1-expose-a-test-seam-in-packages-admin-sdk-tutorial-src-catalo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts
  - packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - The missing-asset loader branch is covered by an automated test that does not rename, delete, or mutate seeded lesson fixtures.
    - `loadLessonCatalog()` still loads the seeded lesson catalog through the default eager Vite glob modules and returns the same runtime shape as Phase 01.
    - The seam does not weaken existing path-safety or missing-asset error behavior.
  artifacts:
    - path: packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts
      provides: Default glob-backed loader seam plus injectable module-map helper
    - path: packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts
      provides: Automated missing-asset coverage using synthetic module maps
  key_links:
    - from: packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts
      to: packages/admin-sdk-tutorial/src/catalog/normalizeCatalog.ts
      via: loadLessonCatalogFromModules delegates normalized entries through the existing normalizer
      pattern: return normalizeCatalog\(lessonEntries\)
    - from: packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts
      to: default eager lesson globs
      via: public loadLessonCatalog delegates to the default module maps
      pattern: return loadLessonCatalogFromModules\(defaultLessonCatalogModules\)
---

<objective>
Expose a minimal test seam inside `loadLessonCatalog.ts` so the remaining manual-only `Missing lesson asset ...` branch can be covered automatically.

Purpose: Close the exact validation gap left by Phase 01 without changing authored lesson fixtures, broadening the package runtime API, or revisiting catalog normalization behavior.
Output: A defaulted module-map seam in the catalog loader and a focused automated spec that proves the missing-asset error branch through synthetic lesson-module inputs.
</objective>

<execution_context>
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-lesson-catalog-authoring-model/01-CONTEXT.md
@.planning/phases/01-lesson-catalog-authoring-model/01-VERIFICATION.md
@.planning/phases/01-lesson-catalog-authoring-model/01-VALIDATION.md
@.planning/phases/01-lesson-catalog-authoring-model/01-03-PLAN.md
@.planning/phases/01-lesson-catalog-authoring-model/01-03-SUMMARY.md
@packages/admin-sdk-tutorial/package.json
@packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts
@packages/admin-sdk-tutorial/src/catalog/catalog.spec.ts
@packages/admin-sdk-tutorial/src/catalog/normalizeCatalog.spec.ts
@packages/admin-sdk-tutorial/src/catalog/types.ts

<interfaces>
From `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts`:
```ts
export function resolveLessonBundleFilePath(bundlePath: string, filePath: string): string;
export function loadLessonCatalog();
```

From `packages/admin-sdk-tutorial/src/catalog/types.ts`:
```ts
export interface TutorialLessonManifest {
  part: TutorialPart;
  chapter: TutorialChapter;
  lesson: TutorialLesson;
  docsLinks: TutorialDocsLink[];
  proseFile: string;
  starterFile: string;
  solutionFile: string;
  supportFiles: string[];
  primaryEditableFile: string;
  scenario: TutorialLessonScenario;
}

export interface TutorialLessonCatalogEntry {
  part: TutorialPart;
  chapter: TutorialChapter;
  lesson: TutorialLesson;
  bundlePath: string;
  prose: string;
  starterFile: string;
  starterCode: string;
  solutionFile: string;
  solutionCode: string;
  primaryEditableFile: string;
  supportFiles: readonly TutorialSupportFile[];
  docsLinks: readonly TutorialDocsLink[];
  scenario: TutorialLessonScenario;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor `loadLessonCatalog.ts` behind a defaulted module-map seam</name>
  <files>packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts</files>
  <read_first>packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts, packages/admin-sdk-tutorial/src/catalog/types.ts, .planning/phases/01-lesson-catalog-authoring-model/01-VERIFICATION.md, .planning/phases/01-lesson-catalog-authoring-model/01-VALIDATION.md, .planning/phases/01-lesson-catalog-authoring-model/01-03-PLAN.md, .planning/phases/01-lesson-catalog-authoring-model/01-03-SUMMARY.md</read_first>
  <action>Keep the existing public `loadLessonCatalog()` runtime behavior, default eager `import.meta.glob` discovery, `resolveLessonBundleFilePath()` guardrails, and exact error strings intact. Inside `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts`, introduce one explicit injectable helper named `loadLessonCatalogFromModules` that accepts both manifest modules and raw asset modules, performs the current `Object.entries(...).map(createLessonCatalogEntry)` work, and returns `normalizeCatalog(lessonEntries)`. Define one default module-map object in the same file, built from the current eager manifest and raw-asset globs, then make the public `loadLessonCatalog()` delegate to that default object. Do not export the new seam through `src/catalog/index.ts`, do not touch `src/lessons/**`, and do not alter `normalizeCatalog.ts` or the catalog record shape unless TypeScript requires a local helper type inside `loadLessonCatalog.ts`.</action>
  <verify>
    <automated>pnpm --dir packages/admin-sdk-tutorial test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` contains `const defaultLessonCatalogModules =`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` contains `export function loadLessonCatalogFromModules(`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` contains `return loadLessonCatalogFromModules(defaultLessonCatalogModules)`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` contains `import.meta.glob("../lessons/**/lesson.manifest.ts"`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` contains `query: "?raw"`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` contains `Missing lesson asset`
  </acceptance_criteria>
  <done>The loader exposes a test-only injectable seam while the production entrypoint still uses the default eager lesson globs and preserves all existing guards and errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused automated coverage for the missing-asset branch without mutating fixtures</name>
  <files>packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts</files>
  <read_first>packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts, packages/admin-sdk-tutorial/src/catalog/catalog.spec.ts, packages/admin-sdk-tutorial/src/catalog/normalizeCatalog.spec.ts, packages/admin-sdk-tutorial/src/catalog/types.ts, packages/admin-sdk-tutorial/package.json, .planning/phases/01-lesson-catalog-authoring-model/01-VERIFICATION.md, .planning/phases/01-lesson-catalog-authoring-model/01-VALIDATION.md</read_first>
  <action>Create a new colocated spec `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts`. Keep `catalog.spec.ts` as the seeded integration regression for the public `loadLessonCatalog()` path. In the new spec, import `loadLessonCatalogFromModules` and build inline synthetic module maps that satisfy `TutorialLessonManifest` without reading or renaming any real lesson files. Add one positive regression test that a complete synthetic bundle normalizes into a single part/chapter/lesson tree, then add the required negative test that omits one manifest-declared asset from the raw asset map and asserts the exact `Missing lesson asset "..." in bundle "..."` error. Reuse a synthetic manifest path under `../lessons/.../lesson.manifest.ts` so the helper exercises the real bundle-path derivation logic, and keep all assertions package-local under Vitest.</action>
  <verify>
    <automated>pnpm --dir packages/admin-sdk-tutorial test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` contains `loadLessonCatalogFromModules`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` contains `Missing lesson asset`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` contains `../lessons/part-99-test/chapter-01-seam/lesson-01-missing-asset/lesson.manifest.ts`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` contains `support/missing.ts`
    - `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` contains `toThrowError`
    - No file under `packages/admin-sdk-tutorial/src/lessons/` is modified by this plan
  </acceptance_criteria>
  <done>Automated coverage proves the missing-asset branch directly through injected module maps, and the seeded lesson fixtures remain untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest module map -> loader seam | Lesson metadata determines which lesson-local assets the loader reads and exposes |
| default runtime loader -> injected test seam | The new helper must not diverge from the production glob-backed path or accidentally widen runtime behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` | mitigate | Route both the default runtime path and the injected test seam through the same `createLessonCatalogEntry()` and `resolveLessonBundleFilePath()` logic so path validation and asset guards cannot be bypassed in one path only |
| T-01.1-02 | Information Disclosure | `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` | mitigate | Keep the new seam local to `loadLessonCatalog.ts` and do not add it to `src/catalog/index.ts`, so package consumers still only see the existing public catalog entrypoint |
| T-01.1-03 | Repudiation | `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.spec.ts` | mitigate | Assert the exact `Missing lesson asset "..." in bundle "..."` message so failures remain attributable to the loader branch under test rather than hidden fixture mutations |
| T-01.1-04 | Denial of Service | `packages/admin-sdk-tutorial/src/catalog/loadLessonCatalog.ts` | mitigate | Preserve the fail-fast missing-asset throw and keep the seeded integration test path green so malformed manifests still abort catalog assembly deterministically |
</threat_model>

<verification>
- `pnpm --dir packages/admin-sdk-tutorial lint:eslint:fix`
- `pnpm --dir packages/admin-sdk-tutorial lint:eslint`
- `pnpm --dir packages/admin-sdk-tutorial lint:types`
- `pnpm --dir packages/admin-sdk-tutorial test:unit -- --run`
- `pnpm --dir packages/admin-sdk exec jest --collectCoverage --watchman=false`
</verification>

<success_criteria>
- The missing-asset error branch is exercised automatically in Vitest without renaming, deleting, or editing any seeded lesson asset under `src/lessons/`
- `loadLessonCatalog()` still resolves the real seeded catalog through the default eager globs and keeps the Phase 01 runtime shape unchanged
- The new seam is scoped to `loadLessonCatalog.ts` and is not exported from the package catalog barrel
- Existing path-rejection behavior and error wording remain intact alongside the new missing-asset test
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-expose-a-test-seam-in-packages-admin-sdk-tutorial-src-catalo/01.1-01-SUMMARY.md`
</output>
