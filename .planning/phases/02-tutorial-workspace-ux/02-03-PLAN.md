---
phase: 02-tutorial-workspace-ux
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts
  - packages/admin-sdk-tutorial/src/workspace/WorkspaceConfirmDialog.vue
  - packages/admin-sdk-tutorial/src/App.vue
autonomous: true
requirements:
  - LESS-03
  - LESS-04
  - LESS-05
must_haves:
  truths:
    - Showing the solution never mutates or replaces the learner draft.
    - Restoring starter and switching away from a dirty lesson both require explicit confirmation.
    - Hiding compare mode returns the learner to the exact local draft they were editing.
  artifacts:
    - packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts
    - packages/admin-sdk-tutorial/src/workspace/WorkspaceConfirmDialog.vue
    - packages/admin-sdk-tutorial/src/App.vue
  key_links:
    - `WorkspaceConfirmDialog.vue` centralizes the destructive confirmation UI so restore and lesson-switch warnings behave consistently.
    - `useTutorialWorkspace.ts` owns compare-mode and pending-navigation state so destructive flows stay testable.
---

<objective>
Add the reversible solution and guarded recovery flows that make the workspace safe to explore.

Purpose: Close the main interaction gaps in Phase 2 by letting learners compare against the solution, recover to starter code, and switch lessons without silent visible-state loss.
Output: Compare mode, destructive confirmations, and dirty-switch protection fully integrated into the workspace shell.
</objective>

<execution_context>
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-tutorial-workspace-ux/02-CONTEXT.md
@.planning/phases/02-tutorial-workspace-ux/02-UI-SPEC.md
@.planning/phases/02-tutorial-workspace-ux/02-RESEARCH.md
@packages/admin-sdk-tutorial/src/App.vue
@packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend workspace state for compare mode and destructive-flow guards</name>
  <files>packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts</files>
  <read_first>packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts, packages/admin-sdk-tutorial/src/catalog/types.ts, .planning/phases/02-tutorial-workspace-ux/02-CONTEXT.md, .planning/phases/02-tutorial-workspace-ux/02-UI-SPEC.md</read_first>
  <action>Extend `useTutorialWorkspace.ts` with explicit reversible and destructive-flow state: `isSolutionVisible`, `pendingLessonId`, `isLessonSwitchConfirmOpen`, `isRestoreStarterConfirmOpen`, `requestLessonOpen(nextLessonId: string)`, `confirmLessonOpen()`, `cancelLessonOpen()`, `requestRestoreStarter()`, `confirmRestoreStarter()`, and `cancelRestoreStarter()`. `requestLessonOpen()` must open the confirmation dialog instead of immediately switching when `isDirty` is `true`; otherwise it opens the target lesson directly. `confirmRestoreStarter()` must replace only the active lesson draft with `starterCode`, persist the change immediately, and leave all other lesson drafts untouched.</action>
  <verify><automated>pnpm --dir packages/admin-sdk-tutorial test:unit -- --run</automated></verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts` contains `pendingLessonId`
    - `packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts` contains `isLessonSwitchConfirmOpen`
    - `packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts` contains `isRestoreStarterConfirmOpen`
    - `packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts` contains `requestLessonOpen`
    - `packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts` contains `confirmRestoreStarter`
    - `packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts` contains `cancelRestoreStarter`
  </acceptance_criteria>
  <done>The workspace state model can support compare mode and safe destructive flows without coupling that logic to DOM events.</done>
</task>

<task type="auto">
  <name>Task 2: Render compare mode and confirmation dialogs in the workspace UI</name>
  <files>packages/admin-sdk-tutorial/src/workspace/WorkspaceConfirmDialog.vue, packages/admin-sdk-tutorial/src/App.vue</files>
  <read_first>packages/admin-sdk-tutorial/src/App.vue, packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts, .planning/phases/02-tutorial-workspace-ux/02-UI-SPEC.md, .planning/phases/02-tutorial-workspace-ux/02-CONTEXT.md</read_first>
  <action>Create `packages/admin-sdk-tutorial/src/workspace/WorkspaceConfirmDialog.vue` as a keyboard-reachable local dialog component with a title slot, body copy, cancel button, and primary action button. Update `App.vue` so the action toolbar includes exact labels `Show solution`, `Hide solution`, and `Restore starter`. On desktop, `Show solution` must split the editor region into a 50/50 compare layout with the learner editor on the left and a read-only solution surface on the right. On narrow screens, keep one editor area visible and add sub-tabs labeled `Your code` and `Solution`. Add selectors `data-testid="show-solution-button"`, `data-testid="hide-solution-button"`, `data-testid="solution-panel"`, `data-testid="restore-starter-button"`, `data-testid="restore-confirm-dialog"`, and `data-testid="lesson-switch-confirm-dialog"`. The lesson-switch dialog copy must include `Your draft stays saved on this device. Open the next lesson or keep editing here?`. The restore dialog copy must include `Restore the starter code for this lesson? Your current draft on this device will be replaced.`.</action>
  <verify><automated>pnpm --dir packages/admin-sdk-tutorial test:unit -- --run</automated></verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Show solution`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Hide solution`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Restore starter`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `data-testid="solution-panel"`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Your draft stays saved on this device`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Restore the starter code for this lesson?`
    - `packages/admin-sdk-tutorial/src/workspace/WorkspaceConfirmDialog.vue` contains `role="dialog"`
  </acceptance_criteria>
  <done>The workspace now supports reversible compare mode and explicit confirmation for destructive or surprising flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| destructive UI actions -> persisted draft state | Restore and lesson-switch confirmations can replace the visible editor state |
| solution source -> compare surface | Solution content must stay read-only and separate from learner code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `packages/admin-sdk-tutorial/src/App.vue` | mitigate | Keep solution rendering read-only and separate from the editable draft binding so compare mode cannot overwrite learner code |
| T-03-02 | Tampering | `packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts` | mitigate | Route restore starter through explicit request/confirm actions and persist only after the destructive action is confirmed |
| T-03-03 | Repudiation | `packages/admin-sdk-tutorial/src/App.vue` | mitigate | Use explicit warning copy for dirty lesson switches so the user understands that the draft is kept locally even when the visible editor changes |
</threat_model>

<verification>
- `pnpm --dir packages/admin-sdk-tutorial test:unit -- --run`
- `pnpm --dir packages/admin-sdk-tutorial lint:eslint`
- `pnpm --dir packages/admin-sdk-tutorial lint:types`
- Final repo-required hygiene after all Phase 2 changes: `pnpm --dir packages/admin-sdk-tutorial lint:eslint:fix`, then `pnpm --dir packages/admin-sdk-tutorial lint:eslint`
- Final repo-required Jest step after all Phase 2 changes: `pnpm --dir packages/admin-sdk test:unit`
</verification>

<success_criteria>
- Learners can reveal the solution without losing their own draft
- Hiding the solution returns the exact draft they were editing
- Restoring starter and switching lessons while dirty always require explicit confirmation
- The workspace keeps Phase 2 scoped: compare and recovery only, no runtime execution features
</success_criteria>

<output>
After completion, create `.planning/phases/02-tutorial-workspace-ux/02-03-SUMMARY.md`
</output>
