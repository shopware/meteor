---
phase: 02-tutorial-workspace-ux
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - packages/admin-sdk-tutorial/package.json
  - packages/admin-sdk-tutorial/src/App.spec.ts
  - packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts
autonomous: true
requirements:
  - LESS-02
  - LESS-03
  - LESS-04
  - LESS-05
  - RUN-01
must_haves:
  truths:
    - Phase 2 browser coverage proves the workspace layout, editing surface, compare mode, restore flow, and refresh recovery end to end.
    - Narrow-screen coverage proves the mobile section tabs preserve the same feature set instead of dropping guidance or editing.
    - Package-local integration tests catch workspace regressions before the slower browser suite runs.
  artifacts:
    - packages/admin-sdk-tutorial/package.json
    - packages/admin-sdk-tutorial/src/App.spec.ts
    - packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts
  key_links:
    - `App.spec.ts` exercises the Vue shell against the real workspace composable wiring.
    - `tutorial-workspace.spec.ts` is the browser-visible requirement proof for all Phase 2 outcomes.
---

<objective>
Add the integration and end-to-end tests that make Phase 2 requirements executable and safe to iterate on.

Purpose: Finish the phase with automated proof for the full workspace flow instead of relying on manual browser checks.
Output: Fast package-local integration coverage plus focused Playwright scenarios for desktop and narrow-screen workspace behavior.
</objective>

<execution_context>
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-tutorial-workspace-ux/02-VALIDATION.md
@packages/admin-sdk-tutorial/package.json
@packages/admin-sdk-tutorial/src/App.vue
@packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts
@packages/admin-sdk-tutorial/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add package-local integration coverage for the workspace shell</name>
  <files>packages/admin-sdk-tutorial/package.json, packages/admin-sdk-tutorial/src/App.spec.ts</files>
  <read_first>packages/admin-sdk-tutorial/package.json, packages/admin-sdk-tutorial/src/App.vue, packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts, packages/admin-sdk-tutorial/vitest.config.ts, .planning/phases/02-tutorial-workspace-ux/02-VALIDATION.md</read_first>
  <action>Add `@vue/test-utils` with version `^2.4.2` to `packages/admin-sdk-tutorial/package.json`. Create `packages/admin-sdk-tutorial/src/App.spec.ts` that mounts `App.vue` and verifies these exact interactions: the first lesson is selected on first render, typing into the editor updates the current draft, `Show solution` reveals a read-only compare surface while preserving the typed draft, `Hide solution` restores the editable surface with the same draft text, and clicking `Restore starter` opens the confirmation dialog before mutating the draft.</action>
  <verify><automated>pnpm --dir packages/admin-sdk-tutorial test:unit -- --run</automated></verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/package.json` contains `"@vue/test-utils": "^2.4.2"`
    - `packages/admin-sdk-tutorial/src/App.spec.ts` contains `Show solution`
    - `packages/admin-sdk-tutorial/src/App.spec.ts` contains `Hide solution`
    - `packages/admin-sdk-tutorial/src/App.spec.ts` contains `Restore starter`
    - `packages/admin-sdk-tutorial/src/App.spec.ts` contains `data-testid="restore-confirm-dialog"`
  </acceptance_criteria>
  <done>The workspace has fast integration coverage for the most failure-prone in-browser interactions.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused Playwright coverage for desktop and narrow-screen workspace flows</name>
  <files>packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts</files>
  <read_first>packages/admin-sdk-tutorial/tests/e2e/lesson-catalog.spec.ts, packages/admin-sdk-tutorial/src/App.vue, packages/admin-sdk-tutorial/playwright.config.ts, packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts, .planning/phases/02-tutorial-workspace-ux/02-VALIDATION.md</read_first>
  <action>Create `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` with two Playwright tests. The desktop test must: open `/`, assert `workspace-shell`, edit the active lesson draft, reveal the solution, hide it, trigger `Restore starter`, cancel once, confirm once, then refresh and assert the last active lesson plus the persisted draft state are restored correctly. The narrow-screen test must set the viewport below `1024px`, switch across the `Lessons`, `Guide`, and `Workspace` tabs, assert the editor and preview remain reachable, and verify that changing lessons from a dirty draft opens `lesson-switch-confirm-dialog` instead of navigating immediately. Use locator assertions only and keep the spec focused on `tests/e2e/tutorial-workspace.spec.ts` so it can be run independently during execution.</action>
  <verify><automated>pnpm --dir packages/admin-sdk-tutorial exec playwright test tests/e2e/tutorial-workspace.spec.ts</automated></verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` contains `workspace-shell`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` contains `show-solution-button`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` contains `restore-confirm-dialog`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` contains `lesson-switch-confirm-dialog`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` contains `mobile-shell-tab`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` contains `1024`
  </acceptance_criteria>
  <done>The phase has browser-visible proof for layout, editing, compare, restore, and refresh-recovery behavior on both desktop and narrow screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace UI -> automated regression suite | The test suite defines the executable contract for Phase 2 behaviors |
| browser reload -> persisted draft and lesson restore | Refresh recovery must preserve drafts without reopening unsafe UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Repudiation | `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` | mitigate | Assert the exact refresh and recovery flow in the browser so resume regressions cannot slip through as manual-only behavior |
| T-04-02 | Denial of Service | `packages/admin-sdk-tutorial/tests/e2e/tutorial-workspace.spec.ts` | mitigate | Keep the browser suite focused to one spec file with locator-only assertions so Phase 2 verification stays fast and stable |
| T-04-03 | Spoofing | `packages/admin-sdk-tutorial/src/App.spec.ts` | mitigate | Verify that compare and restore controls expose the correct labels and confirmation surfaces, reducing the chance of misleading UI regressions |
</threat_model>

<verification>
- `pnpm --dir packages/admin-sdk-tutorial test:unit -- --run`
- `pnpm --dir packages/admin-sdk-tutorial exec playwright test tests/e2e/tutorial-workspace.spec.ts`
- `pnpm --dir packages/admin-sdk-tutorial lint:eslint`
- `pnpm --dir packages/admin-sdk-tutorial lint:types`
- Final repo-required hygiene after all Phase 2 changes: `pnpm --dir packages/admin-sdk-tutorial lint:eslint:fix`, then `pnpm --dir packages/admin-sdk-tutorial lint:eslint`
- Final repo-required Jest step after all Phase 2 changes: `pnpm --dir packages/admin-sdk test:unit`
</verification>

<success_criteria>
- Phase 2 requirements have browser-visible automated proof, not just unit coverage
- Refresh recovery, compare mode, restore starter, and dirty lesson switch guards are all exercised end to end
- Narrow-screen parity is verified so mobile layout changes cannot silently drop key learning surfaces
- The phase is ready for `/gsd-execute-phase` without relying on manual validation for core user journeys
</success_criteria>

<output>
After completion, create `.planning/phases/02-tutorial-workspace-ux/02-04-SUMMARY.md`
</output>
