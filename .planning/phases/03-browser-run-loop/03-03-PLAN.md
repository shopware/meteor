---
phase: 03-browser-run-loop
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts
  - packages/admin-sdk-tutorial/src/App.vue
  - packages/admin-sdk-tutorial/src/App.spec.ts
  - packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts
autonomous: true
requirements:
  - RUN-02
  - RUN-03
  - RUN-04
  - RUN-05
  - SHELL-05
must_haves:
  truths:
    - The learner can click `Run preview` and see the isolated preview update without any auto-run on load or edit.
    - The workspace keeps the last successful preview visible after edits and marks it as stale until the learner runs again.
    - Compile or runtime failures replace the preview surface with an error state and keep diagnostics visible below it.
    - `Restart preview` preserves the current draft and starts a new isolated preview session.
  artifacts:
    - packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts
    - packages/admin-sdk-tutorial/src/App.vue
    - packages/admin-sdk-tutorial/src/App.spec.ts
    - packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts
  key_links:
    - `usePreviewRuntime.ts` composes the controller, session, bridge, and host pieces from Plans 01 and 02.
    - `App.vue` renders the Phase 3 controls, status pill, preview states, and diagnostics copy from `03-UI-SPEC.md`.
    - `tutorial-run-loop.spec.ts` proves browser-local run, diagnostics, restart, and sandbox isolation end to end.
---

<objective>
Wire the Phase 3 run loop into the existing tutorial workspace and prove it in the browser.

Purpose: Deliver the user-facing run/restart/diagnostics experience on top of the browser-local compiler and isolated iframe foundation from the earlier Phase 3 plans.
Output: A dedicated preview-runtime composable, the updated workspace preview card, integration coverage, and Playwright proof for run-loop behavior.
</objective>

<execution_context>
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jannisleifeld/Sites/meteor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-browser-run-loop/03-CONTEXT.md
@.planning/phases/03-browser-run-loop/03-UI-SPEC.md
@.planning/phases/03-browser-run-loop/03-VALIDATION.md
@.planning/phases/02-tutorial-workspace-ux/02-03-SUMMARY.md
@.planning/phases/02-tutorial-workspace-ux/02-04-SUMMARY.md
@packages/admin-sdk-tutorial/src/App.vue
@packages/admin-sdk-tutorial/src/App.spec.ts
@packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts
@packages/admin-sdk-tutorial/src/runtime/preview-controller.ts
@packages/admin-sdk-tutorial/src/runtime/preview-session.ts
@packages/admin-sdk-tutorial/src/runtime/preview-bridge.ts
@packages/admin-sdk-tutorial/src/runtime/preview-host.ts

<interfaces>
From `packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts`:
```ts
return {
  activeLessonId,
  activeLesson,
  currentDraft,
  updateDraft,
  isDirty,
  flushPendingPersistence,
};
```

Plan 01 output contract:
```ts
buildPreviewModule({ lesson, draft, sessionId, sessionToken }) =>
  { status: "success", moduleCode, diagnostics: [] } |
  { status: "compile-error", diagnostics };
```

Plan 02 output contract:
```ts
createPreviewSession();
createPreviewIframe(session, locationId);
matchesActivePreviewEvent(event, session, iframeWindow);
attachPreviewHost(session, iframeWindow);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated preview runtime composable and wire the workspace preview card</name>
  <files>packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts, packages/admin-sdk-tutorial/src/App.vue</files>
  <read_first>packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts, packages/admin-sdk-tutorial/src/App.vue, packages/admin-sdk-tutorial/src/workspace/useTutorialWorkspace.ts, packages/admin-sdk-tutorial/src/runtime/preview-controller.ts, packages/admin-sdk-tutorial/src/runtime/preview-session.ts, packages/admin-sdk-tutorial/src/runtime/preview-bridge.ts, packages/admin-sdk-tutorial/src/runtime/preview-host.ts, .planning/phases/03-browser-run-loop/03-CONTEXT.md, .planning/phases/03-browser-run-loop/03-UI-SPEC.md, .planning/phases/03-browser-run-loop/03-VALIDATION.md</read_first>
  <action>Create `packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts` so run-state stays separate from persisted lesson drafts per D-03/D-06/D-07. It must expose exact refs/actions for the view layer: `status`, `diagnostics`, `hasStartedSession`, `hasUnrunChanges`, `runPreview(lesson, draft)`, `restartPreview(lesson, draft)`, `notifyDraftChanged(draft)`, `previewIframe`, and `dispose()`. `runPreview(...)` must call the Plan 01 compiler only when the user clicks `Run preview`; it must never auto-run on mount or on draft edits (D-01/D-02). `restartPreview(...)` must create a fresh session/iframe via the Plan 02 helpers while preserving the caller-provided draft string (D-06/D-07). Update `packages/admin-sdk-tutorial/src/App.vue` to replace the Phase 2 placeholder preview card with the exact Phase 3 UI contract: primary button copy `Run preview`, secondary button copy `Restart preview`, idle heading/body copy from `03-UI-SPEC.md`, a single status pill in the header, the stale-state pill `Changes not run yet`, a persistent diagnostics panel with `data-testid="preview-diagnostics"`, and a neutral preview surface container with `data-testid="preview-surface"`. On failure, replace the preview contents with the Phase 3 error copy instead of continuing to show stale success output (D-05). Keep the layout as editor-above-preview in the right pane and do not add fake-admin chrome (D-09/D-10).</action>
  <verify><automated>./node_modules/.bin/vitest --config packages/admin-sdk-tutorial/vitest.config.ts run packages/admin-sdk-tutorial/src/App.spec.ts</automated></verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts` contains `hasUnrunChanges`
    - `packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts` contains `"compile-error"`
    - `packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts` contains `"runtime-error"`
    - `packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts` contains `notifyDraftChanged`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Run preview`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Restart preview`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Changes not run yet`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `Diagnostics will appear here after you run the lesson.`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `No issues from the last run.`
    - `packages/admin-sdk-tutorial/src/App.vue` contains `data-testid="preview-diagnostics"`
  </acceptance_criteria>
  <done>The workspace exposes a real Phase 3 preview card and run-state model without collapsing draft persistence into runtime state.</done>
</task>

<task type="auto">
  <name>Task 2: Prove explicit run, diagnostics, restart, and isolation with integration and browser tests</name>
  <files>packages/admin-sdk-tutorial/src/App.spec.ts, packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts</files>
  <read_first>packages/admin-sdk-tutorial/src/App.spec.ts, packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts, packages/admin-sdk-tutorial/src/App.vue, packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts, packages/admin-sdk-tutorial/src/runtime/preview-controller.spec.ts, packages/admin-sdk-tutorial/src/runtime/preview-session.spec.ts, .planning/phases/03-browser-run-loop/03-VALIDATION.md, .planning/phases/03-browser-run-loop/03-UI-SPEC.md</read_first>
  <action>Expand `packages/admin-sdk-tutorial/src/App.spec.ts` using the existing mocked CodeMirror pattern so it covers the exact user-facing rules from D-01 through D-07: no preview auto-run on mount; `Run preview` starts the preview and shows a success surface; editing after success leaves the previous preview visible and shows `Changes not run yet`; compile failures render `Compile error`; runtime failures render `Runtime error`; and `Restart preview` creates a new preview session while the textarea keeps the learner draft. Create `packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts` with real browser proof for the same flows plus RUN-05/SHELL-05: the spec must assert that the preview iframe exists only after a run, has a `sandbox` attribute equal to `allow-scripts`, updates after explicit runs, preserves the draft after restart, and never references StackBlitz or TutorialKit URLs in the page content or frame URL. Use stable selectors `data-testid="run-preview-button"`, `data-testid="restart-preview-button"`, `data-testid="preview-status-pill"`, `data-testid="preview-surface"`, and `data-testid="preview-iframe"`.</action>
  <verify><automated>./node_modules/.bin/vitest --config packages/admin-sdk-tutorial/vitest.config.ts run packages/admin-sdk-tutorial/src/App.spec.ts && ./node_modules/.bin/playwright test --config packages/admin-sdk-tutorial/playwright.config.ts packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts && ./node_modules/.bin/jest --config packages/admin-sdk-tutorial/jest.config.cjs</automated></verify>
  <acceptance_criteria>
    - `packages/admin-sdk-tutorial/src/App.spec.ts` contains `run-preview-button`
    - `packages/admin-sdk-tutorial/src/App.spec.ts` contains `Changes not run yet`
    - `packages/admin-sdk-tutorial/src/App.spec.ts` contains `Compile error`
    - `packages/admin-sdk-tutorial/src/App.spec.ts` contains `Runtime error`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts` contains `preview-iframe`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts` contains `allow-scripts`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts` contains `StackBlitz`
    - `packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts` contains `TutorialKit`
  </acceptance_criteria>
  <done>The Phase 3 run loop is proven in both integration and browser contexts, including the isolation boundary and repo-required Jest step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace UI -> preview runtime composable | The host UI must never treat stale or failed preview state as current learner output |
| browser automation -> preview iframe | Tests must verify the actual sandboxed frame boundary, not a mocked substitute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | `packages/admin-sdk-tutorial/src/runtime/usePreviewRuntime.ts` | mitigate | Track `lastRunDraft` separately from the current draft and mark stale previews with `hasUnrunChanges` instead of silently replacing the successful snapshot |
| T-03-09 | Denial of Service | `packages/admin-sdk-tutorial/src/App.vue` | mitigate | Keep compile and runtime errors in a dedicated diagnostics panel so the tutorial shell stays mounted and interactive after failed runs |
| T-03-10 | Spoofing | `packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts` | mitigate | Assert the actual iframe `sandbox` value and session replacement behavior in the browser so isolation regressions are caught automatically |
</threat_model>

<verification>
- `./node_modules/.bin/vitest --config packages/admin-sdk-tutorial/vitest.config.ts run packages/admin-sdk-tutorial/src/App.spec.ts`
- `./node_modules/.bin/playwright test --config packages/admin-sdk-tutorial/playwright.config.ts packages/admin-sdk-tutorial/tests/e2e/tutorial-run-loop.spec.ts`
- `./node_modules/.bin/jest --config packages/admin-sdk-tutorial/jest.config.cjs`
- Final repo-required lint autofix after all Phase 3 work: `./node_modules/.bin/eslint packages/admin-sdk-tutorial --fix`
- Final repo-required lint verification after autofix: `./node_modules/.bin/eslint packages/admin-sdk-tutorial`
- Final repo-required type check: `./node_modules/.bin/vue-tsc --project packages/admin-sdk-tutorial/tsconfig.json --noEmit`
</verification>

<success_criteria>
- The learner can run and restart the isolated preview explicitly from the workspace without losing the current draft
- Draft edits after a successful run keep the last success visible while clearly marking it as stale
- Compile and runtime failures render in the dedicated diagnostics panel and replace the preview surface state instead of crashing or hiding the shell
- Browser tests prove the run loop stays browser-local and sandboxed
</success_criteria>

<output>
After completion, create `.planning/phases/03-browser-run-loop/03-03-SUMMARY.md`
</output>
