---
"@shopware-ag/meteor-component-library": minor
---

Add `mt-container`: a layout container that fills the available width up to a maximum width and centers itself, with the sizes `s` (680px), `m` (960px, default) and `l` (1280px) taken from the new `--container-size-s`, `--container-size-m` and `--container-size-l` custom properties, and an `as` prop for the rendered element
