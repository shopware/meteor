---
"@shopware-ag/meteor-icon-kit": minor
---

Replace the generated SCSS stylesheet with pure CSS. Import `icons/meteor-icon-kit.css` — stable filename, fixed `px` units. The old `icons/meteor-icon-kit.scss` path still works as a plain-CSS copy but is deprecated.
