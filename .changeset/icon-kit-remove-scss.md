---
"@shopware-ag/meteor-icon-kit": minor
---

Replace the generated SCSS stylesheet with pure CSS. Import `icons/meteor-icon-kit.css` — stable filename, fixed `px` units. The hash-suffixed `meteor-icon-kit-<hash>.css` file is no longer generated. The old `icons/meteor-icon-kit.scss` path still works as a plain-CSS copy but is deprecated.
