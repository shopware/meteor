---
"@shopware-ag/meteor-component-library": minor
---

Add `MtBreadcrumb`, `MtBreadcrumbItem`, `MtBreadcrumbLink` and `MtBreadcrumbSeparator` for hierarchy navigation with sizes `xs` and `s` and automatic truncation and collapsing of crumbs on narrow containers
